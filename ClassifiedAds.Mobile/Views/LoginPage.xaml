<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:vm="clr-namespace:ClassifiedAds.Mobile.ViewModels"
             x:Class="ClassifiedAds.Mobile.Views.LoginPage"
             Title="Login"
             BackgroundColor="White">

    <ScrollView>
        <VerticalStackLayout Spacing="25" Padding="30" VerticalOptions="Center">

            <Button Text="✕" 
                    TextColor="Gray"
                    BackgroundColor="Transparent"
                    HorizontalOptions="End"
                    Clicked="OnCloseClicked"
                    HeightRequest="40"
                    WidthRequest="40"
                    FontSize="20"/>

            <Label Text="Welcome Back" 
                   FontSize="32" 
                   FontAttributes="Bold" 
                   TextColor="Black"
                   HorizontalOptions="Center" 
                   Margin="0,0,0,20"/>

            <VerticalStackLayout Spacing="5">
                <Label Text="Email" TextColor="#555" FontSize="14" Margin="5,0,0,0"/>
                <Border StrokeThickness="0" BackgroundColor="#F0F0F0" StrokeShape="RoundRectangle 12" HeightRequest="50" Padding="15,0">
                    <Entry Text="{Binding Email}" 
                           Placeholder="Enter your email" 
                           Keyboard="Email"
                           TextColor="Black"
                           PlaceholderColor="Gray"
                           VerticalOptions="Center"/>
                </Border>
            </VerticalStackLayout>

            <VerticalStackLayout Spacing="5">
                <Label Text="Password" TextColor="#555" FontSize="14" Margin="5,0,0,0"/>
                <Border StrokeThickness="0" BackgroundColor="#F0F0F0" StrokeShape="RoundRectangle 12" HeightRequest="50" Padding="15,0">
                    <Entry Text="{Binding Password}" 
                           IsPassword="True" 
                           Placeholder="Enter your password" 
                           TextColor="Black"
                           PlaceholderColor="Gray"
                           VerticalOptions="Center"/>
                </Border>
            </VerticalStackLayout>

            <Label Text="{Binding ErrorMessage}" 
                   IsVisible="{Binding HasError}"
                   TextColor="Red" 
                   HorizontalOptions="Center"/>

            <Button Text="Login" 
                    Command="{Binding LoginCommand}" 
                    BackgroundColor="Black" 
                    TextColor="White"
                    FontAttributes="Bold"
                    CornerRadius="25" 
                    HeightRequest="55" 
                    Margin="0,10,0,0"/>

            <HorizontalStackLayout HorizontalOptions="Center" Spacing="5" Margin="0,10,0,0">
                <Label Text="Don't have an account?" TextColor="Gray"/>
                <Label Text="Register" TextColor="#007AFF" FontAttributes="Bold">
                    <Label.GestureRecognizers>
                        <TapGestureRecognizer Command="{Binding GoToRegisterCommand}"/>
                    </Label.GestureRecognizers>
                </Label>
            </HorizontalStackLayout>

            <ActivityIndicator IsRunning="{Binding IsBusy}" Color="Black" HorizontalOptions="Center"/>

        </VerticalStackLayout>
    </ScrollView>
</ContentPage>